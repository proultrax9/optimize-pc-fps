<UserControl x:Class="FpsGodPc.App.Controls.TweakRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource CardBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12">
        <DockPanel>
            <Button Content="{Binding IsToggled, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToEnableLabelConverter}}"
                    Command="{Binding ToggleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    DockPanel.Dock="Right"
                    Width="90"
                    Height="30"
                    Margin="10,0,0,0"
                    Background="{StaticResource AccentBrush}"
                    Foreground="{StaticResource VoidBrush}"
                    BorderBrush="{x:Null}"/>
            <StackPanel>
                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontSize="13"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextBrush}"/>
                <TextBlock Text="{Binding Description, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           Margin="0,2,0,4"
                           FontSize="11"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           TextWrapping="Wrap"/>
                <TextBlock Text="{Binding Risk, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontSize="10"
                           Foreground="{StaticResource AccentBrush}"/>
            </StackPanel>
        </DockPanel>
    </Border>
</UserControl>
