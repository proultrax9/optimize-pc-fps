<UserControl x:Class="FpsGodPc.App.Controls.PageHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <Border Background="{StaticResource HeroGlowBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="22"
            Padding="24"
            Effect="{StaticResource AccentGlowEffect}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="{Binding Eyebrow, ElementName=Root}"
                           Foreground="{StaticResource AccentBrush}"
                           Margin="2,0,0,8">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource LabelText}">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Eyebrow, ElementName=Root}" Value=""><Setter Property="Visibility" Value="Collapsed"/></DataTrigger>
                                <DataTrigger Binding="{Binding Eyebrow, ElementName=Root}" Value="{x:Null}"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <TextBlock Text="{Binding Title, ElementName=Root}" Style="{StaticResource PageTitle}"/>
                <TextBlock Text="{Binding Subtitle, ElementName=Root}"
                           Style="{StaticResource PageSubtitle}"
                           MaxWidth="760"
                           HorizontalAlignment="Left"
                           Margin="0,8,0,0"/>
            </StackPanel>

            <ContentPresenter Grid.Column="1"
                              Content="{Binding Action, ElementName=Root}"
                              VerticalAlignment="Center"
                              Margin="24,0,0,0"/>
        </Grid>
    </Border>
</UserControl>
