<UserControl x:Class="FpsGodPc.App.Views.ProfilesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctrl="clr-namespace:FpsGodPc.App.Controls">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="28,24,28,28">
            <ctrl:PageHeader Eyebrow="PROFILES" Title="{Binding Title}" Subtitle="{Binding Subtitle}" Margin="0,0,0,18"/>

            <TextBlock Text="{Binding WatcherStatusText}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,14" TextWrapping="Wrap"/>

            <ItemsControl ItemsSource="{Binding Profiles}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardSurface}" Padding="16,14" Margin="0,0,0,10">
                            <DockPanel>
                                <Button DockPanel.Dock="Right"
                                        Content="{Binding DataContext.ApplyLabel, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        Command="{Binding DataContext.ApplyNowCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding Id}"
                                        Style="{StaticResource PrimaryButton}"
                                        VerticalAlignment="Center"
                                        Margin="10,0,0,0"/>
                                <CheckBox DockPanel.Dock="Right"
                                          Content="{Binding DataContext.WatcherLabel, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                          IsChecked="{Binding WatcherEnabled, Mode=OneWay}"
                                          Command="{Binding DataContext.ToggleWatcherCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                          CommandParameter="{Binding Id}"
                                          Margin="0" VerticalAlignment="Center"/>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource CardHeading}"/>
                                    <TextBlock Text="{Binding Executable}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0" FontFamily="{StaticResource MonoFont}"/>
                                </StackPanel>
                            </DockPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
